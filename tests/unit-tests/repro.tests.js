/* global describe, it */
/* eslint-disable prefer-arrow-callback, func-names, no-shadow */

import { expect } from 'chai';


describe('Map transactions to a portfolio history', function () {
  it('calculates the deltas base on transaction history', function () {
    expect(true).to.equal(true);
  });
});
